#!/bin/bash
if test -z $1
then
	exit 0
fi

if [ "$1" = "purge" ]; then
  echo " Removing weborf config file."
  rm -f /etc/weborf.conf*
  if [ -x "/usr/bin/ucf" ]; then
    ucf --purge /etc/weborf.conf
  fi
fi

